<resources>

    <!-- Base application theme. -->
    <style name="MaterialTheme" parent="ParentMaterialTheme" >

    </style>

    <!-- Parent Theme for all all pre-lollipop and post lollipop versions -->
    <style name="ParentMaterialTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- colorPrimary : Base Color of the toolbar-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--colorPrimaryDark: Base Color of the status bar(the topmost part) -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">#ff5f52</item>
        <!--<item name="colorControlActivated">#ff5f52</item> -->
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light" >
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

</resources>
